(trigger
    (key [[alt][cmd]B]))

(name [Build Package])

(only-in source.go)

(input document)

(output html)

(save nothing)

(script [#!/usr/bin/env ruby

require "#{ENV['TM_BUNDLE_SUPPORT']}/gomate"

gomate "gopack", "Building"
])